schema_version: 1
scope: buro
parent_scope: platform
ip_owner: "Buro Built"
visibility: buro
description: >
  Universe scope — capabilities owned by and visible only to the buro universe.
  Only repos whose ai-manifest.yaml declares universe: buro may require plugins
  from this scope; the resolver fails closed otherwise.
surfaces: [claude-code, devin, cursor, codex, openhands]
promotion_policy:
  auto_promote: false
  cost_threshold_usd: null
