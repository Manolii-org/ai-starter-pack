schema_version: 1
scope: manolii
parent_scope: platform
ip_owner: "Manolii Pty Ltd"
visibility: manolii
description: >
  Universe scope — capabilities owned by and visible only to the manolii universe.
  Only repos whose ai-manifest.yaml declares universe: manolii may require plugins
  from this scope; the resolver fails closed otherwise.
surfaces: [claude-code, devin, cursor, codex, openhands]
promotion_policy:
  auto_promote: false
  cost_threshold_usd: null
