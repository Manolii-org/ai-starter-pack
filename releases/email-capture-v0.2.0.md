# email-capture v0.2.0

Hosted capture lane: `mode=hosted` + `backend=hosted`, HTTPS SPI, env-only bearer token, `canary` CLI, and optional fidelity. Hermetic Mailpit/MailDev/Inbucket behavior is unchanged and remains fail-closed in staging/production.
